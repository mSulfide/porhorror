# Описание API
Здесь описано всё АПИ, используемое в приложении, с описанием структур данных


**Содержание**
1. Общее
    * 1.1. Адрес сервера
    * 1.2. Используемый протокол
2. Структуры данных
    * 2.1. Общий формат ответа
    * 2.2. Пользователь
    * 2.3. Сообщение
    * 2.4. Точка
    * 2.5. Окружность
    * 2.6. Сплайны
    * 2.7. Игровой объект
3. Список запросов
    * 3.1. Общие ошибки
4. Подробно
    * 4.1. login
    * 4.2. logout
    * 4.3. registration
    * 4.4. sendMessage
    * 4.5. getMessages
    * 4.6. derivative
    * 4.7. spline
    * 4.8. getCirclesIntersection
    * 4.9. changeInventory
    * 4.10. startGame
    * 4.11. updateGroups
    * 4.12. createGroup
    * 4.13. deleteGroup
    * 4.14. joinToGroup
    * 4.15. leaveGroup
    * 4.16. dropFromGroup
    * 4.17. updateGroup
    * 4.18. autoLogin
    * 4.19. updateScene
    * 4.20. action



## 1. Общее
### 1.1. Адрес сервера
`http://nopainnogame.local/api`

### 1.2. Используемый протокол
API полностью реализовано на http(s). 

Формат возвращаемых значений `JSON`.

Все методы, если это особо не оговорено, имеют тип `GET`.


## 2. Структуры данных
### 2.1. Общий формат ответа
`T` - какие-то данные. В случае успешного ответа возвращается `result = 'ok'` и поле `data` с данными.

В случае ошибки возвращается `result = 'error'` и поле `error` с кодом и текстом ошибки
```
Answer<T>: {
    result: 'ok' | 'error';
    data?: T;
    error?: {
        code: number;
        text: string;
    };
}
```

### 2.2. Пользователь
```
User: {
    id: number;
    token: string;
    name: string;
}
```

### 2.3. Сообщение
```
Message: {
    message: string;
    author: string;
    created: string;
}
```

### 2.4. Точка
```
Point: {
    x: float;
    y: float;
}
```
**Примечание** Point: Представляет точку на плоскости с координатами x и y

### 2.5. Лобби
```
Lobby: {
    id: number;
    name: string;
    status: string;
}
```

### 2.6. Сплайн
```
Spline: {
    $coefficients[] = [
            'a' => $a1,
            'b' => $b1,
            'c' => $c1,
            'd' => $y1,
        ];
}
```
**Примечание** Spline: Представляет коэффициенты, которые образуют сплайн на плоскости 

### 2.7. Игровой объект
```
GameObject: {
    gameId: number;
    position: Point;
    velocity: Point;
    radius: float;
    angle: float;
}
```

## 3. Список запросов
| Название | О чем |
| - | - |
| login | Авторизация пользователя |
| autoLogin | Автоматический вход по токену |
| logout | Логаут пользователя |
| registration | Регистрация пользователя |
| sendMessage | Отправить сообщение в чат |
| getMessages | Получить сообщения в чате |
| getCirclesIntersection | Получить точку пересечения двух окружностей |
| spline | Получить коэффициент для получения сплайнов |
| startGame | Запускает сессию созданной группы |
| updateGroups | Получить список групп |
| createGroup | Создать новую группу |
| deleteGroup | Удалить группу |
| joinToGroup | Присоединиться к группе |
| leaveGroup | Покинуть группу |
| dropFromGroup | Выгнать из группы |
| updateScene | Обновить сцену |
| action | Действие |

### 3.1. Общие ошибки
* `101` - если не передан параметр `method`
* `102` - если переданное значение в параметре `method` не обрабатывается
* `242` - не переданы все необходимые параметры


## 4. Подробно
### 4.1. login
Авторизация пользователя в системе

**Параметры**
```
{
    login: string; - логин пользователя
    hash: string; - контрольная сумма, равная hash = md5(nd5(login + password) + rnd)
    rnd: number; - случайно целое число
}
```
**Успешный ответ**
```
    Answer<User>
```
**Ошибки**
* `1002` - неверный пароль
* `1005` - неверный логин


### 4.2. logout
Вылогин пользователя из системы

**Параметры**
```
{
    token: string
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `1003` - неверный пароль


### 4.3. registration
Зарегистрировать нового ползователя

**Параметры**
```
{
    login: string; - логин пользователя
    password: string; - хеш пароля: md5(login + password)
    name: string; - имя пользователя
}
```
**Успешный ответ**
```
    Answer<User>
```
**Ошибки**
* `1001` - такой логин уже существует
* `1004` - ошибка при регистрации пользователя


### 4.4. sendMessage
Послать сообщение в чат

**Параметры**
```
{
    token: string; - токен
    message: string; - текст сообщения
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован


### 4.5. getMessages
Получить все сообщения чата

**Параметры**
```
{
    token: string; - токен
    hash: string; - хеш-сумма чата
}
```
**Успешный ответ**
```
    Answer<{
        messages: Message[]; - список сообщений
        hash: string; - новый хеш чата
    }>
```
**Примечание**: в случае отсутствия новых сообщений будет ответ:
```
    Answer<
        hash: string; - новый хеш чата
    >
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован


### 4.6. derivative
Получение производной

**Параметры**
```
{
    "func": function,  // функция, для которой необходимо вычислить производную
    "x": number,       // точка, в которой вычисляется производная
    "eps": number      // малое значение, используемое для приближения
}
```
**Успешный ответ**
```
    Answer<{
    "derivative": number  // значение производной функции в точке x
    }>
```
**Ошибки**
* `303` - недостаточно точек для построения сплайна


### 4.7. spline
Получение сплайновой функции

**Параметры**
```
{
    "points": [
        {
            "x": number,  // координата x точки
            "y": number   // координата y точки
        }
    ]
}
```
**Успешный ответ**
```
    Answer<{
    "coefficients": [
        {
            "a": number,  // коэффициент a для сегмента
            "b": number,  // коэффициент b для сегмента
            "c": number,  // коэффициент c для сегмента
            "d": number   // коэффициент d для сегмента
        }
    ]
}>
```

### 4.8. getCirclesIntersection
Получить точку пересечения двух окружностей

**Параметры**
```
{
    circle1: {
    x: float; - координата X центра первой окружности
    y: float; - координата Y центра первой окружности
    radius: float; - радиус первой окружности
  };
  circle2: {
    x: float; - координата X центра второй окружности
    y: float; - координата Y центра второй окружности
    radius: float; - радиус второй окружности
  };
}
```

**Успешный ответ**
```
{
    x: float; - координата X точки пересечения
    y: float; - координата Y точки пересечения
}
```

**Ошибки**
* `242` - неверные параметры. Отсутствует один из параметров circle1 или circle2
* `243` - неверные параметры. Отсутствует один из параметров x, y, radius в circle1 или circle2

### 4.9. changeInventory
Изменение инвентаря

**Параметры**
```
{
    "itemId": number; //идентификатор предмета, который нужно переместить.
    "fromEquipment": boolean; //логическое значение, указывающее, убирается ли предмет из экипировки или из инвентаря.
}
```
**Успешный ответ**
```
    Answer<{
        "changeInventory": boolean; //Возвращает true, если операция была успешной
                                    //Возвращает false, если произошла ошибка 
    }>
```
**Ошибки**
* `666` - некорректные параметры


### 4.10. startGame
Запустить сессию

**Параметры**
```
{
    token: string; - токен
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован
* `1105` - лобби не существует.

### 4.11. updateGroups
Получить все доступные лобби

**Параметры**
```
{
    token: string; - токен
    hash: string; - хеш-сумма групп
}
```
**Успешный ответ**
```
    Answer<{
        lobbies: Lobby[]; - список групп
        hash: string; - новый хеш групп
    }>
```
**Примечание**: в случае отсутствия новых сообщений будет ответ:
```
    Answer<
        hash: string; - новый хеш групп
    >
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован

### 4.12. createGroup
Создать новую группу

**Параметры**
```
{
    token: string; - токен
    name: string; - название группы
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `1105` - лобби не существует
* `713` - пользователь уже является создателем группы

### 4.13. deleteGroup
Удалить группу

**Параметры**
```
{
    token: string; - токен
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `711` - пользователь не является создателем группы
* `715` - лобби не открыто
* `1105` - лобби не существует

### 4.14. joinToGroup
Присоединиться к группе

**Параметры**
```
{
    lobbyId: number; - идентификатор лобби
    token: string; - токен
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `710` - пользователь уже состоит в группе
* `715` - лобби не открыто
* `1105` - лобби не существует

### 4.15. leaveGroup
Покинуть группу (и удалить группу, если её покидает создатель)

**Параметры**
```
{
    token: string; - токен
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `712` - пользователь не является членом группы
* `714` - пользователь является создателем группы и не может покинуть её

### 4.16. dropFromGroup
Выгнать пользователя из группы

**Параметры**
```
{
    userId: number; - идентификатор пользователя
    token: string; - токен
}
```
**Успешный ответ**
```
    Answer<true>
```
**Ошибки**
* `711` - пользователь не является создателем группы
* `1105` - лобби не существует

### 4.17. updateGroups
Получить лобби

**Параметры**
```
{
    token: string; - токен
    hash: string; - хеш группы
}
```
**Успешный ответ**
```
    Answer<{
        lobbies: User[]; - список пользователей
        hash: string; - новый хеш группы
    }>
```
**Примечание**: в случае отсутствия изменений будет ответ:
```
    Answer<
        hash: string; - хеш группы
    >
```
**Ошибки**
* `705` - невалидный токен. Пользователь не авторизован

### 4.18. autoLogin
Автоматический вход по токену

**Параметры**
```
{
    token: string; - токен
}
```
**Успешный ответ**
```
    Answer<{
        id: number; - идентификатор пользователя
        name: string; - имя пользователя
        token: number; - токен
    }>
```

**Ошибки**
* `1006` - невалидный токен

### 4.19. updateScene
Обновление сцены

**Параметры**
```
{
    token: number; - токен
    hash: string; - хэш-сумма игры
}
```
**Успешный ответ**
```
    Answer<{
        scene: GameObject[];
        hash: string;
    }>
```

**Ошибки**
* `805` - игры не существует

### 4.20. action
Действие

**Параметры**
```
{
    token: number; - токен
}
```
**Успешный ответ**
```
    Answer<true>
```

**Ошибки**
* `810` - игрока не существует