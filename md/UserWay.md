## Содержание:
1. Общее описание
2. Экраны
    * 2.1. Логин
    * 2.2. Регистрация
    * 2.3. Главное меню
    * 2.4. Игра
3. Окна
    * 3.1. Инвентарь
    * 3.2. Лобби
    * 3.3. Список лобби
    * 3.4. Информация о пользователе
    * 3.5. Редактор персонажа
    * 3.6. Настройки
    * 3.7. Свойства шмотки
4. Компоненты
    * 4.1. Слот
    * 4.2. Персонаж
5. Методы
    * 5.1. Получить персонажа по пользователю
6. Типы
    * 6.1. Персонаж
## 1. Общее описание
## 2. Экраны
### 2.1. Логин
* Поля ввода:
    * `Логин` — строка
    * `Пароль` — строка
* Кнопки:
    * `Зарегистрироваться` — перемещает на **2.2**
    * `Войти` — в случае успешной авторизации перемещает на **2.3**
### 2.2. Регистрация
* Поля ввода:
    * `Имя` — строка
    * `Логин` — строка
    * `Пароль` — строка
    * `Подтвердить пароль` — строка
* Кнопки:
    * `Зарегистрироваться` — в случае успешной регистрации перемещает на **2.3**
    * `Войти` — перемещает на **2.1**
### 2.3. Главное меню
* Окна:
    * `Пользователь` 
        * Отображает **3.4**
        * Кнопка `Редактировать персонажа` — открывает **3.5**
    * `Инвентарь` — отображает **3.1**
    * `Группа` — отображает **3.2**
    * `Список групп` — отображает **3.3**
* Кнопки:
    * `Настройки` — открывает **3.6**
    * `Выход` — в случае успешного выхода перемещает на **2.1**
### 2.4. Игра
## 3. Окна
### 3.1. Инвентарь
* Далее используется **4.1** с свойствами:
    * `Иконка` = Иконка шмотки
    * `onMouseOver` = открывает **3.7**
    * `onMouseOut` = закрывает **3.7**
* `Снаряжение` — 3 штуки **4.1**: `onClick` = снять шмотку
* `Инвентарь` — n x m штук **4.1**: `onClick` = надеть шмотку
### 3.2. Лобби
* `Пользователи` — список пользователей в лобби:
    * **4.2**: `Персонаж` = **5.1**
    * Имя пользователя
    * Кнопка `Выгнать` — выгоняет пользователя
* Кнопки:
    * `Запустить` — есть только у создателя группы, запускает игру
    * `Покинуть` — выходит из лобби
### 3.3. Список лобби
* `Группы` — список из лобби:
    * Название
    * Количество участников
    * Кнопка `Присоединиться` — присоединяет пользователя к лобби
* Кнопка `Создать группу` — создает лобби
### 3.4. Информация о пользователе
* **4.2**: `Персонаж` = **5.1**
* Имя пользователя
* Рейтинг
* Место в рейтенге
### 3.5. Редактор персонажа
* Далее используется **4.1** с свойствами:
    * `Иконка` = иконка одежды
    * `onClick` = применяет текущий внешний вид
    * `onMouseOver` = одевает одежду
    * `onMouseOut` = одевает предыдущую одежду
* `Доступная одежда` — матрица из **4.1**
* **4.2**: `Персонаж` = **5.1**
### 3.6. Настройки
### 3.7. Свойства шмотки
* Описание
## 4. Компоненты
### 4.1. Слот
* Входные параметры:
    * `Иконка` — спрайт
    * События:
        * `onClick` — щелчок левой кнопкой мыши на элементе
        * `onMouseOver` — курсор наводится на элемент
        * `onMouseOut` — курсор покидает элемент
* Отображает свойство `Иконка`
* Вызывает события
### 4.2. Персонаж
* Входные параметры: `Персонаж` — **6.1**
* Отображает предварительный просмотр персонажа в игре
## 5. Методы
### 5.1. Получить персонажа по пользователю
* Возвращает **6.1** по пользователю
## 6. Типы
### 6.1. Персонаж
* `Шапка` — id одежды
* `Прическа` — id одежды
* `Цвет кожи` — id одежды
* `Руки` — id одежды